var currentTooltip;

        // Function to strip HTML tags from the text
        function stripHTMLTags(html) {
            var tmp = document.createElement("div");
            tmp.innerHTML = html;
            return tmp.textContent || tmp.innerText || "";
        }

        function showFullText(event, text) {
            hideFullText(); // Hide any open tooltip

            var tooltip = document.createElement("span");
            tooltip.textContent = text;

            tooltip.style.position = "fixed";
            tooltip.style.backgroundColor = "#555";
            tooltip.style.color = "#fff";
            tooltip.style.textAlign = "center";
            tooltip.style.borderRadius = "6px";
            tooltip.style.padding = "5px";
            tooltip.style.opacity = "0";
            tooltip.style.transition = "opacity 0.3s";

            document.body.appendChild(tooltip);

            // Calculate the tooltip's width
            var tooltipWidth = tooltip.getBoundingClientRect().width;

            // Calculate the appropriate position for the tooltip
            var positionTop = event.pageY - tooltip.offsetHeight - 10;
            var positionLeft = event.pageX + 10;
            var positionRight = positionLeft + tooltipWidth;

            // Adjust tooltip position if it goes beyond the viewport width
            var viewportWidth = window.innerWidth;
            if (positionRight > viewportWidth - 10) {
                positionLeft = viewportWidth - tooltipWidth - 10;
            }

            tooltip.style.top = positionTop + "px";
            tooltip.style.left = positionLeft + "px";

            // Set the tooltip to be visible
            setTimeout(function() {
                tooltip.style.opacity = "1";
            }, 1);

            // Store the current tooltip
            currentTooltip = tooltip;
        }

        function hideFullText() {
            if (currentTooltip) {
                // Set the tooltip to be hidden
                currentTooltip.style.opacity = "0";

                // Remove the tooltip after the transition
                setTimeout(function() {
                    currentTooltip.remove();
                }, 300);

                // Reset currentTooltip variable
                currentTooltip = null;
            }
        }
