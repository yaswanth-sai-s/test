<!DOCTYPE html>
<html>
<head>
    <title>Hover Display Text</title>
    <style>
        .truncated-text {
            position: relative;
            display: inline-block;
            max-width: 200px; /* Adjust the width as per your requirement */
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            cursor: pointer;
        }

        .truncated-text:hover:after {
            content: attr(data-full-text);
            position: absolute;
            top: -10px; /* Adjust the vertical position of the tooltip */
            left: 100%; /* Adjust the horizontal position of the tooltip */
            background-color: #555;
            color: #fff;
            padding: 5px;
            border-radius: 6px;
            opacity: 1;
            max-width: 400px; /* Maximum width of the tooltip */
            max-height: 300px; /* Maximum height of the tooltip */
            overflow: auto; /* Add scroll to the tooltip */
            visibility: visible;
            z-index: 9999; /* Set a high z-index to display above other elements */
        }

        .truncated-text:hover span {
            visibility: hidden;
        }
    </style>
</head>
<body>
    {% for item in items %}
        <span
            class="truncated-text"
            data-full-text="{{ item.text|safe }}"
        >
            <span>{{ item.text|truncatechars:20|safe }}</span>
        </span>
    {% endfor %}
</body>
</html>
